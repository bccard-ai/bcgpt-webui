/**
 * @fileoverview `$lib` barrel entry point.
 *
 * Place files you want to import through the `$lib` alias in this folder.
 * This file intentionally stays minimal — deep imports (`$lib/utils/…`,
 * `$lib/stores/…`, etc.) are preferred for tree-shaking.
 */
